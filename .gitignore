# Things we don't want checked into a Git repository

# The user configs supplied to the packer build shouldn't be checked in
vars/vars*.json
vars/agent-vars*.json
vars/console-vars*.json

# Ignore the file indicating passwords have been autogenerated
.passwords

# Ignore the EC2 information for starting the Grinder Console
ec2-console.ami
ec2-console.instance
ec2-agent.ami
ec2-agent-*.instance

# Ignore all the log files generated from the Packer.io build
logs
